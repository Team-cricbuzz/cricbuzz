package com.example.demo.swagger;

public class SwaggerConfig {

	@Bean
	public Docket productApi() {
        return new Docket(DocumentationType.SWAGGER_2)
                .select()
                .apis(RequestHandlerSelectors.basePackage("com.RankingService.controller"))
                .paths(regex("/rank"))
                .build();
    }
}
